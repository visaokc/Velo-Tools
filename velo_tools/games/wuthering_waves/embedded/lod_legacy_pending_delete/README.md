# Legacy WWMI LOD implementation pending deletion

This directory is an inert snapshot of the former per-draw LOD implementation.
It generated per-LOD vertex Blend buffers and paired them with native LOD bone
constant buffers. The active implementation lives in `../lod/` and must not
import this package. Delete this directory after the replacement has completed
in-game acceptance.
